#
# top-level help
#
exec provisioner1 -h
! stderr .
cmp stdout top-level-help.txt

#
# install help
#
exec provisioner1 install -h
! stderr .
cmp stdout install-help.txt

#
# configure help
#
exec provisioner1 configure -h
! stderr .
cmp stdout configure-help.txt

#
# list
#
exec provisioner1 list
! stderr .
stdout 'A -- desc A\n'
stdout 'B -- desc B\n'

-- top-level-help.txt --
Usage: provisioner1 <command>

🌼 florist 🌺 - a simple provisioner

Flags:
  -h, --help    Show context-sensitive help.

Commands:
  install      install the provisioner1 role
  configure    configure the provisioner1 role
  list         list the flowers and their files

Run "provisioner1 <command> --help" for more information on a command.
-- install-help.txt --
Usage: provisioner1 install

install the provisioner1 role

Flags:
  -h, --help    Show context-sensitive help.
-- configure-help.txt --
Usage: provisioner1 configure

configure the provisioner1 role

Flags:
  -h, --help               Show context-sensitive help.

      --settings=STRING    Dynamic settings file (JSON)
      --secrets=STRING     Secrets file (JSON)
