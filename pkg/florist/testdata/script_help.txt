#
# top-level help
#
exec provisioner-1 -h
! stderr .
cmp stdout top-level-help.txt

#
# install help
#
exec provisioner-1 install -h
! stderr .
cmp stdout install-help.txt

#
# configure help
#
exec provisioner-1 configure -h
! stderr .
cmp stdout configure-help.txt

-- top-level-help.txt --
provisioner-1 -- A 🌼 florist 🌺 provisioner

Usage: provisioner-1 <command> [options]

Commands:

 list          list the flowers and their files
 install       install the flowers
 configure     configure the flowers

Options:

 --log-level LOG-LEVEL    set the log level (default: INFO)

 -h, --help               Print this help and exit

-- install-help.txt --
provisioner-1 install -- install the flowers

Usage: provisioner-1 install [options]

Options:

 -h, --help    Print this help and exit

-- configure-help.txt --
provisioner-1 configure -- configure the flowers

Usage: provisioner-1 configure [options]

Options:

 --settings SETTINGS    Settings file (JSON) (default: /opt/florist/config.json)

 -h, --help             Print this help and exit

